import { TaskFilterItemComponent } from './task-filter-item.component';

describe('TaskFilterItemComponent', () => {
  const component = new TaskFilterItemComponent();

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
