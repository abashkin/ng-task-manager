import { AddTaskFormComponent } from './add-task-form.component';

describe('AddTaskFormComponent', () => {
  let component: AddTaskFormComponent;

  beforeEach(() => {
    component = new AddTaskFormComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
